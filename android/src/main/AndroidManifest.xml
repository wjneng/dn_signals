<manifest xmlns:android="http://schemas.android.com/apk/res/android"
  package="com.datanexus.dn_signals">
  <permission
    android:name="${applicationId}.gdt.qq.SEND_PERMISSION"
    android:protectionLevel="signature" />
  <permission
    android:name="${applicationId}.gdt.qq.RECEIVE_PERMISSION"
    android:protectionLevel="signature" />

  <uses-permission android:name="android.permission.INTERNET" />
  <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
  <uses-permission android:name="android.permission.READ_PHONE_STATE" />
  <uses-permission android:name="${applicationId}.gdt.qq.SEND_PERMISSION" />
  <uses-permission android:name="${applicationId}.gdt.qq.RECEIVE_PERMISSION" />

  <application>
    <provider
      android:name="com.qq.gdt.action.GDTInitProvider"
      android:authorities="${applicationId}.GDTInitProvider"
      android:exported="false" />
  </application>
</manifest>
